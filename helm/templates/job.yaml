apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "devnopes.name" . }}
  namespace: {{ .Values.job.namespace }}
  labels:
    {{- include "devnopes.labels" . | nindent 4 }}
spec:
  ttlSecondsAfterFinished: {{ .Values.job.ttlSecondsAfterFinished }}
  backoffLimit: 0
  template:
    metadata:
      labels:
        {{- include "devnopes.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "devnopes.name" . }}
      restartPolicy: Never

      {{- with .Values.job.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- if .Values.cniConfMount.enabled }}
      volumes:
        - name: cni-conf
          hostPath:
            path: {{ .Values.cniConfMount.hostPath }}
            type: DirectoryOrCreate
      {{- end }}

      containers:
        - name: diagnostics
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          {{- if .Values.cniConfMount.enabled }}
          volumeMounts:
            - name: cni-conf
              mountPath: /host/etc/cni/net.d
              readOnly: true
          {{- end }}

          env:
            - name: CLUSTER_DOMAIN
              value: {{ .Values.config.clusterDomain | quote }}
            - name: DNS_SLOW_THRESHOLD
              value: {{ .Values.config.dnsSlowThreshold | quote }}
            - name: DNS_TIMEOUT
              value: {{ .Values.config.dnsTimeout | quote }}
            - name: CONNECT_TIMEOUT
              value: {{ .Values.config.connectTimeout | quote }}
            - name: LOG_SCAN_SECONDS
              value: {{ .Values.config.logScanSeconds | quote }}
            {{- if .Values.networkPathTest.enabled }}
            - name: TARGET_SERVICE
              value: {{ .Values.networkPathTest.service | quote }}
            - name: TARGET_NAMESPACE
              value: {{ .Values.networkPathTest.namespace | quote }}
            - name: TARGET_PORT
              value: {{ .Values.networkPathTest.port | quote }}
            {{- end }}
            {{- if .Values.overrides.coreDns.namespace }}
            - name: COREDNS_NAMESPACE
              value: {{ .Values.overrides.coreDns.namespace | quote }}
            {{- end }}
            {{- if .Values.overrides.coreDns.extraLabelSelectors }}
            - name: COREDNS_EXTRA_LABELS
              value: {{ .Values.overrides.coreDns.extraLabelSelectors | join "," | quote }}
            {{- end }}
            {{- if .Values.overrides.kubeProxy.namespace }}
            - name: KUBE_PROXY_NAMESPACE
              value: {{ .Values.overrides.kubeProxy.namespace | quote }}
            {{- end }}
            {{- if .Values.overrides.kubeProxy.daemonSetName }}
            - name: KUBE_PROXY_DAEMONSET_NAME
              value: {{ .Values.overrides.kubeProxy.daemonSetName | quote }}
            {{- end }}
            {{- if .Values.overrides.kubeProxy.extraLabelSelectors }}
            - name: KUBE_PROXY_EXTRA_LABELS
              value: {{ .Values.overrides.kubeProxy.extraLabelSelectors | join "," | quote }}
            {{- end }}
            {{- if .Values.cniDetection.extraNamespaces }}
            - name: CNI_EXTRA_NAMESPACES
              value: {{ .Values.cniDetection.extraNamespaces | join "," | quote }}
            {{- end }}
            {{- if .Values.cniDetection.extraLabels }}
            - name: CNI_EXTRA_LABELS
              value: {{ .Values.cniDetection.extraLabels | join "," | quote }}
            {{- end }}

          resources:
            {{- toYaml .Values.resources | nindent 12 }}

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop: ["ALL"]
